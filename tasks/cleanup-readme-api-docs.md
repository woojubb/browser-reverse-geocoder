# Task: Update README API Documentation

## Description
Update readme.md to reflect the new `init()` signature that accepts both URL strings and GeoJSON objects directly.

## Status
- [ ] Update Usage section with new `init(source)` signature
- [ ] Add example for direct GeoJSON object usage
- [ ] Remove outdated `@turf/turf` references
